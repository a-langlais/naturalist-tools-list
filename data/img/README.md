# Icônes

Placez ici les icônes référencées par la colonne `icon` de `data/tools.csv`.

La colonne doit contenir uniquement le nom du fichier, par exemple :

```csv
cartoflore.png
```

Formats recommandés : `.png`, `.jpg`, `.webp` ou `.svg`.
